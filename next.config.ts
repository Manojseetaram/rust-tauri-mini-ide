// // next.config.js
// const path = require("path");

// module.exports = {
//   webpackDevMiddleware: (config: { watchOptions: { ignored: any[]; }; }) => {
//     config.watchOptions = {
//       ignored: [path.resolve(__dirname, "projects/**")]
//     };
//     return config;
//   }
// };
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;